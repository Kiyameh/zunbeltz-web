---
import type { Mountain } from "@/schemas/locations";

interface Props {
  mountain: Mountain;
}

const { mountain } = Astro.props;

const { altitude, location } = mountain;
---

<div class="card">
  <div class="header">
    <h3 class="title">Datos de la montaña</h3>
  </div>
  <div class="grid">
    {
      altitude && (
        <div class="grid-item">
          <span class="label">Altitud</span>
          <span class="value">{altitude} m</span>
        </div>
      )
    }
    {
      location && (
        <div class="grid-item">
          <span class="label">Localidad</span>
          <span class="value">{location}</span>
        </div>
      )
    }
  </div>
</div>

<style>
  /* Card */
  .card {
    background: var(--color-surface-100);
    border: 2px solid var(--color-border-100);
    border-radius: var(--radius-xl);
    padding: 1.5rem;
    font-family: var(--font-paragraph);
  }

  .header {
    margin-bottom: 1rem;
  }

  .title {
    font-family: var(--font-title);
    font-size: 1.25rem;
    margin: 0;
  }

  /* Grid */
  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1rem;
  }

  .grid-item {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .label {
    font-size: 0.875rem;
    color: var(--color-content-200);
    font-weight: 500;
  }

  .value {
    font-size: 0.9375rem;
    color: var(--color-content-400);
    font-weight: 600;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .grid {
      grid-template-columns: 1fr;
    }
  }
</style>
